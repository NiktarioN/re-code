import handleRightActiveBlockLoaded from './right-active-block';
import addToggleBlockTitle from '../../features/right-active-block/add-toggle-block-title';

const initHandleRightActiveBlockLoaded = () => {
  handleRightActiveBlockLoaded([addToggleBlockTitle]);
};

export default initHandleRightActiveBlockLoaded;