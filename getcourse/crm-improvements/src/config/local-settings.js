const LOCAL_SETTINGS = {
  tasks: {
    quickDelay: {
      useMoscowTime: false,
    },
    autoNotify: {
      isEnabled: false,
      minutes: 5,
    },
    draftComment: '',
    reloadAfterComment: false,
  },
};

export default LOCAL_SETTINGS;